package server

import "github.com/labstack/echo"

func GetAccounts(c echo.Context) {
	// userID := c.Param(":userID")

	// allAccounts, err := repo.Account.Get(userID)
	// if err != nil {
	// log.Fatalln("Error receving accounts ", "code: ", err.Code, "message: ", err.Error())
	// }
}
